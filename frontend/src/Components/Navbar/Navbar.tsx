import { Box } from '@mui/material'
import React, { FC } from 'react'

interface Props {}

const Navbar = (props: Props) => {
  return (
    <Box sx={{width:'9.rem', height:'55px', background:'red'}}></Box>
  )
}

export default Navbar